services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: cabanas
      POSTGRES_USER: cabanas_user
      POSTGRES_PASSWORD: cabanas_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    depends_on:
      - db

volumes:
  postgres_data: